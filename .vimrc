packloadall

syntax on
set number

"whenever you split vertically, appear on the right
set splitright
"whenever you split horizontally, appear on the bottom
set splitbelow


" Required for theme
set termguicolors

colorscheme miramare

" Start NERDTree and put the cursor back in the other window
" autocmd VimEnter * NERDTree | wincmd p

" Airline theme
let g:airline_theme='bubblegum'
